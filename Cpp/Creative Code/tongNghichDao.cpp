#include <bits/stdc++.h>

using namespace std;
long long n;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;

    vector<long long> a(n);
    for (int i = 0; i < n; i++){
        cin >> a[i];
    }

    return 0;
}
