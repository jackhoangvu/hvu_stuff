#include <bits/stdc++.h>

using namespace std;

int main()
{
  ios::sync_with_stdio(0);
  cin.tie(0);
  string s; cin>>s;
  // bool ok1=false;//contain 0 1 2 3 4 6 8
  // bool ok2=true;//contain 8or4 for 0, 1 for 6, 2 for 4, 3 for 2, 4 for 8
  // for(char c:s){
  //   if((c-'0')==0||(c-'0')==1||(c-'0')==2||(c-'0')==3||(c-'0')==4||(c-'0')==6||(c-'0')==8){ok1=true; break;}
  // }
  // if(!ok1){cout<<"NO"; return 0;}
  // if()
  // cout<<"YES";
  // solution not working, ermmm...
  //problem is like you have a number that's length is <=10^5 which i think you must use string, and then you must output "YES" or "NO" as output if the digits can be rearranged so that the number is divisible by 8 which I mean only "YES" and "NO" is like weird??? Like I can just maybe printing out randomly and get some score??? idk bro i think that's the problem, im sure it's YES/NO tho but like someone can just printing randomly tho???
  return 0;
}
